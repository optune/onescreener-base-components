import { Telegram } from '@styled-icons/boxicons-logos/Telegram'

import { toSquidInkIconComponent } from '../mixins/toSquidInkIcon.jsx'

export const TelegramIcon = toSquidInkIconComponent(Telegram, 'telegram', {
  ['no-stroke']: true,
})
