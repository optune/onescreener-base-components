import { Picasa } from '@styled-icons/entypo-social/Picasa'

import { toSquidInkIconComponent } from '../mixins/toSquidInkIcon.jsx'

export const PicasaIcon = toSquidInkIconComponent(Picasa, 'picasa', {
  ['no-stroke']: true,
})
