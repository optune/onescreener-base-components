/* eslint-disable max-len, react/display-name, react/prop-types */

import { TextDocument } from '@styled-icons/entypo/TextDocument'

import { toSquidInkIconComponent } from '../mixins/toSquidInkIcon.jsx'

export const MediaKitIcon = toSquidInkIconComponent(TextDocument, 'visual-graph', {
  ['no-stroke']: true,
})
