import { Dropbox } from '@styled-icons/boxicons-logos/Dropbox'

import { toSquidInkIconComponent } from '../mixins/toSquidInkIcon.jsx'

export const DropboxIcon = toSquidInkIconComponent(Dropbox, 'dropbox', {
  ['no-stroke']: true,
})
